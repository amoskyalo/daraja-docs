# Introduction

Welcome to Safaricom Daraja Documentation!

---

We believe in investing in the future, working hard towards the creation of technologies that positively impact the society and most importantly transforming the lives of those around, which is a core value that we hold dear. With more innovators and thinkers emerging daily, we endeavour to aid our partners in their road to discovery through interesting and encouraging platforms that enable them to achieve their business goals.

---

## What We Offer

In this release, we have exposed API endpoints for accessing M-Pesa services; we have M-Pesa API endpoints for <Highlight>B2C</Highlight>, <Highlight>C2B</Highlight> and <Highlight>B2B</Highlight>. Our APIs are built on REST; data entities are represented as HTTP resources and are accessed using HTTP verbs, majorly GET and POST. API request parameters and responses - including errors - are encoded in JSON.

---

## Standards & Compatibility

Our APIs response status codes and error codes comply with HTTP status codes as defined in RFC 2616. You can invoke our API endpoints using REST clients like Postman or SoapUI and command line tools like Curl and Node.js.

---

## Getting Started

To get you started, we have provided code samples in:

- curl
- Ruby
- PHP
- Python
- NodeJS
- Java

Sample JSON responses for each APIs request have also been availed to help you understand the expected response format and structure.

<Note type="tip">
    <NoteInfo>
        Ready to begin? Start with our Terminologies section to understand key concepts, then move to Development Setup
        for technical implementation guidance.
    </NoteInfo>
</Note>
